{
  "name": "wasm-browser-example",
  "version": "1.0.0",
  "description": "WebAssembly in the browser — Rust compiled to WASM with Mandelbrot, Fibonacci, and hashing demos",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build-wasm.js",
    "prestart": "npm run build",
    "start": "node server.js",
    "pretest": "npm run build",
    "test": "node test.js"
  },
  "keywords": [],
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  }
}
